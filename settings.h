#ifndef PLXR_SETTINGS_H
#define PLXR_SETTINGS_H

/* compile-time settings, a lot of these could actually be made
 * into run-time settings... TODO: make more of these dynamic.
 */

#define SERVER_TIMEOUT      (1)
#define CLIENT_TIMEOUT      (1)
#define CLIENT_MAX          (200)
#define CLIENT_MAX_ATTEMPTS (32)
#define SERVER_PORT         (8080)

#endif
